"use strict"

class CockpitMenu extends BaseScene {

    constructor(config, lastScene) {
        super(config, lastScene);
    }

    init () {
        super.init();
    }

    preload () {
        super.preload();

    }
    
    create () {
        super.create();

    }

    update () {
        super.update();
    }
}